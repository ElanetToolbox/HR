@model IEnumerable<HR.Data.Models.Evaluation>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    @Html.Partial("_Section", item)
}

</table>
