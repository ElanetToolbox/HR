@model IEnumerable<HR.Data.Models.Employee>

@{
    ViewBag.Title = "Index";
}

<h2>Προσωπικό</h2>

<table class="table">
    <tr>
        <th>
            AA
        </th>
        <th>
            AA
        </th>
        <th>
            Ονοματεπώνυμο
        </th>
        <th>
            Ημ/νία Πρόσληψης
        </th>
        <th>
            Ημ/νία Γέννησης
        </th>
        <th>
            Διεύθυνση
        </th>
        <th>
            Είδικότητα
        </th>
        <th>
            Θέση
        </th>
        <th></th>
    </tr>
@{int i = 1;
foreach (var item in Model)
{
    <tr onclick="location.href = '@(Url.Action("Details", "Employees", new { id = item.ID }))'">
        <td>
            @i
        </td>
        <td>
            <img src="@item.photo_blob_html_image" width="50" height="50" style="border-radius:50%"/>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FullName)
        </td>
        <td>
            @Convert.ToDateTime(item.DateHired).ToString("dd/MM/yyyy")
            @*@Html.DisplayFor(modelItem => item.DateHired)*@
        </td>
        <td>
            @Convert.ToDateTime(item.DoB).ToString("dd/MM/yyyy")
           @* @Html.DisplayFor(modelItem => item.DoB)*@
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Directorate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Specialty)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Position)
        </td>
    </tr>
    i++;
}
    }
</table>
